const API_SUFFIX = '/api/v1'

export const LOGIN = `${API_SUFFIX}/auth/login`

export const USER = `${API_SUFFIX}/users`
export const INVOICES = `${API_SUFFIX}/invoices`
export const UP_FILE = `${API_SUFFIX}/upload-files/admin`
export const PAYMENT = `${API_SUFFIX}/payments`
export const BANK_TRANSFER = `${API_SUFFIX}/bank-transfer`
export const PAY_CHECK = `${API_SUFFIX}/pay-checks`
export const HELP = `${API_SUFFIX}/help-request`

export const COMPANY = `${API_SUFFIX}/companies`
export const BANKS = `${API_SUFFIX}/banks`

export const TAXES = `${API_SUFFIX}/taxes`
